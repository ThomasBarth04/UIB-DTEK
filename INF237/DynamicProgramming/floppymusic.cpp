// =============================================================
// FLOPPY MUSIC — (Stub / work in progress)
// =============================================================
// This file is an unfinished skeleton.
// The full problem would involve reading some sequence and
// solving a DP over a 1D grid with state tracking.
// =============================================================

#include <iostream>

using namespace std;
bool solveBlock(int t, int n, int lines[]);
int main(int argc, char *argv[]) {
  int f_num;
  cin >> f_num;
  cout << f_num << endl;
  return 0;
}

// solveBlock: Intended to solve one block of the puzzle.
// t = time steps, n = number of lines, lines[] = the input sequence.
// Returns true if the block is solvable.
// (Not yet implemented)
bool solveBlock(int t, int n, int lines[]) {
  int current_pos = 0;

  return false;
}
